# Issue Tracker

The intended issue tracker is GitHub Issues for the public `ariadne-thread` repository.

The remote is not connected yet in this workspace. Until it is, record durable planning context in `PRD.md`, `CONTEXT.md`, and `docs/adr/`; do not invent a parallel local issue format unless the user asks for one.

When the GitHub remote exists, skills that create or triage issues should use the GitHub CLI or GitHub MCP tooling against that repository.
